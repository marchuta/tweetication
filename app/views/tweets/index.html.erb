<%= render "shared/header" %>

<div class ="contents row" >
 <% @tweets.each do |tweet|%>
    <%= render partial: "tweet", locals: { tweet: tweet } %>
  <% end %>
</div>

<%= link_to('/tweets/new', class: 'post-btn') do %>
  <span class='post-btn-text'>投稿する</span>
  
<% end %>
